import { getDeviceType } from '../../common/js/helpers';
import Component from '../../common/js/component';

class Solution extends Component {
  constructor(nRoot) {
    super(nRoot, 'solution');
  }

  destroy() {
  }
}

export default Solution;
