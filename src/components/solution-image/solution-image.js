import FullHeight from '../full-height/full-height';

class SolutionImage extends FullHeight {
  constructor(nRoot) {
    super(nRoot, 'solution-image');
  }

  destroy() {

  }
}

export default SolutionImage;
