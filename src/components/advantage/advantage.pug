mixin advantage(args, i)
    .advantage&attributes(attributes)
        .advantage__wrapper
            .advantage__count!= (i).toString().padStart(2, '0')
            .advantage__title!=args.title
            .advantage__text!=args.text
